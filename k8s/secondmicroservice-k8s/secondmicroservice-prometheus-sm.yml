apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: secondmicroservice-app
  namespace: poc
  labels:
    team: poc
spec:
  selector:
    matchLabels:
      app: secondmicroservice
  endpoints:
    - port: web
      path: /prometheusMetrics
