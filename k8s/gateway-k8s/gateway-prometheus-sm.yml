apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: gateway-app
  namespace: poc
  labels:
    team: poc
spec:
  selector:
    matchLabels:
      app: gateway
  endpoints:
    - port: web
      path: /prometheusMetrics
